import os

def ensure_tools_folder_exists():
    # نام پوشه
    folder_name = "tools"
    # نام فایل نگهدارنده (نقطه اول آن باعث مخفی شدن و استاندارد بودن آن می‌شود)
    keep_file = ".keep"
    
    # مسیر کامل فایل
    file_path = os.path.join(folder_name, keep_file)

    # ۱. اگر پوشه وجود نداشت، آن را بساز
    if not os.path.exists(folder_name):
        os.makedirs(folder_name)
        print(f"✅ پوشه '{folder_name}' با موفقیت ساخته شد.")
    
    # ۲. اگر فایل .keep وجود نداشت، آن را بساز
    if not os.path.exists(file_path):
        with open(file_path, 'w') as f:
            pass  # فقط یک فایل خالی ایجاد می‌کند
        print(f"✅ فایل نگهدارنده '{file_path}' ساخته شد تا پوشه حذف نشود.")
    else:
        print(f"ℹ️ پوشه tools و فایل نگهدارنده از قبل وجود دارند.")

# --- اجرای تابع در ابتدای برنامه ---
if __name__ == "__main__":
    ensure_tools_folder_exists()
    
    # ==========================================
    # کدها و دستورات اصلی برنامه شما در ادامه می‌آید...
    # ==========================================
    print("🚀 اجرای ادامه برنامه...")
